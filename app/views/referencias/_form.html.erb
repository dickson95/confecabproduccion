<%= simple_form_for(@referencia) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :referencia %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
