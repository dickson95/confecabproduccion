<fieldset class="row">
    <div class="col-md-6">
      <%= f.input :contacto %>
    </div>
    <div class="col-md-5">
      <%= f.input :cargo %>
    </div>
    <div class="col-md-1">
      <%= f.hidden_field :_destroy %>
      <%= link_to '<i class="fa fa-times"></i>'.html_safe, '#', class: "remove_fields btn btn-danger btn-sm", style: "margin-top: 25px;" %>
    </div>


    <!--Teléfonos-->
    <div class="col-md-12">
      <%= f.fields_for :telefonos do |telefono| %>
          <%= render 'telefonos/telefono_fields', f: telefono %>
      <% end %>
      <%= fields '<i class="fa fa-phone"></i> Teléfono'.html_safe, f, :telefonos, "btn" %>
    </div>

  <hr>
    <!--Correos-->
    <div class="col-md-12">
      <%= f.fields_for :correos do |correo| %>
          <%= render 'correos/correo_fields', f: correo %>
      <% end %>
      <%= fields '<i class="fa fa-envelope-o"></i> Correo'.html_safe, f, :correos, "btn" %>
    </div>
  <hr>
</fieldset>